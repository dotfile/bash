# vim: set filetype=bash :
# Bash aliases (~/.bash/aliases.sh)
# Add to .bashrc, `. ~/.bash/thisfile.sh`

# Better usage or output
alias ls="ls -Bhv --color=always --group-directories-first --hide=*pyc"
alias less="less -r" # color support
alias tmux="tmux -2" # 256color
alias thttpd='thttpd -nos -M 0' # symlinks outside cwd, nocache
alias wget='wget -p -nd' # Page reqs and no directories
alias vi='vi -p' # Open multiple files in tabs
alias diff='colordiff'
alias ll='ls -la | ack "\-\>"' # List symlinks
alias lh='ls -d .*' # List hidden

# Shortcuts 
alias v='vi'
alias ack='ack-grep'
alias go='gnome-open'
alias paste='xclip -o -selection clipboard'
alias sighup='kill -HUP'
alias ~='cd ~'
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

# Python Virtualenv
alias virtualize='virtualenv virtualenv'
alias activate='source virtualenv/bin/activate'

# Git Shortcuts
alias commit='git commit -a'
alias push='git push github master'
alias pull='git pull github master'

# Larger, custom stuff
alias resolution='xdpyinfo  | grep "dimensions:"'
alias reso='resolution'

# Mimic vim
alias :q='exit'
alias :qa='exit'

# Typofix
alias cd.='cd ..'
alias cd..='cd ..'
alias clea='clear'
alias clera='clear'
alias cear='clear'
alias clar='clear'
alias cler='clear'
alias celar='clear'
alias claer='clear'
alias lcear='clear'
alias eit='exit'
alias exi='exit'
alias ext='exit'
alias eixt='exit'
alias unt='grunt'
alias kilall='killall'
alias killal='killall'
alias s='ls'
alias sl='ls'
alias lsl='ls -l'
alias lsa='ls -a'
alias ls-l='ls -l'
alias nmp='npm'
alias sshadd='ssh-add'
alias ssh-ad='ssh-add'

# Reminders
alias quit='echo "No, exit. Get it right."'

# Funny
alias fucking='sudo '

